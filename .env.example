# Memory MCP server process to spawn (stdio transport)
#
# Most Memory MCP servers are started via a command that speaks MCP over stdin/stdout.
# Examples you might use locally:
# - MEMORY_MCP_COMMAND=npx
# - MEMORY_MCP_ARGS=["-y","@modelcontextprotocol/server-memory"]
MEMORY_MCP_COMMAND=memory-mcp

# Optional args. Prefer JSON array format: ["--flag","value"]
MEMORY_MCP_ARGS=[]

# Optional working directory for the spawned process
# MEMORY_MCP_CWD=/home/you/path
